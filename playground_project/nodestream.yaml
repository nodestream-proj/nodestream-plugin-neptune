scopes:
  test:
    pipelines:
      - test.yaml
      - test2.yaml

targets:
  db-one:
    database: neo4j
    database_name: neo4j
    username: neo4j
    password: neo4j123
    uri: bolt://localhost:7687
  db-two:
    database: neo4j
    database_name: neo4j
    username: neo4j
    password: neo4j123
    uri: bolt://localhost:7688
  db-nept:
    database: neptune
    host: https://<NEPTUNE_ENDPOINT>:<PORT>
    region: <AWS_REGION>
  db-p8:
    database: neptune_analytics
    graph_id: <GRAPH_IDENTIFIER>
    region: <AWS_REGION>
