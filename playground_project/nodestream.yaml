scopes:
  test:
    pipelines:
      - test.yaml
      - test2.yaml

targets:
  db-one:
    database: neptune
    mode: database
    host: https://<NEPTUNE_ENDPOINT>:<PORT>
    region: <AWS_REGION>
  db-two:
    database: neptune
    mode: analytics
    graph_id: <GRAPH_IDENTIFIER>
