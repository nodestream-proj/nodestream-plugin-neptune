scopes:
  test:
    pipelines:
      - test.yaml
      - test2.yaml

targets:
  neptune-db:
    database: neptune
    mode: database
    host: https://<NEPTUNE_ENDPOINT>:<PORT>
    region: <AWS_REGION>
  neptune-analytics:
    database: neptune
    mode: analytics
    graph_id: <GRAPH_IDENTIFIER>
    region: <AWS_REGION>
